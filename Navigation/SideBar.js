import { View, Text, ScrollView} from 'react-native'
import React from 'react'

const SideBar = () => {
  return (
    <ScrollView>
      <Text>SideBar</Text>
    </ScrollView>
  )
}

export default SideBar